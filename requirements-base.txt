# Base requirements without pyannote.audio to avoid dependency resolution issues
# Voice activity detection
webrtcvad>=2.0.10

# Audio processing
audioop-lts==0.2.2
pydub>=0.25.1
numpy>=1.21.0
librosa>=0.9.2
soundfile>=0.10.0
PyYAML>=5.4.1

# Transformers and datasets for model/benchmarking
transformers>=4.50.0
datasets>=2.0.0

# Pytorch and Torchaudio
# Pin PyTorch to < 2.6 to avoid weights_only=True issue with pyannote.audio
# Note: torchaudio will be installed separately in Dockerfile with --no-deps to avoid version conflict
torch<2.6.0
# torchaudio is installed in Dockerfile with --no-deps to work around version conflict

# Environment variables
python-dotenv==1.1.1

